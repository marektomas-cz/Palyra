Run the ACP stdio bridge and legacy compatibility shim

Usage: palyra acp [OPTIONS]
       palyra acp <COMMAND>

Commands:
  shim  Run the legacy NDJSON ACP compatibility shim

Options:
      --grpc-url <GRPC_URL>

      --token <TOKEN>

      --principal <PRINCIPAL>

      --device-id <DEVICE_ID>

      --channel <CHANNEL>

      --session-key <SESSION_KEY>

      --session-label <SESSION_LABEL>

      --require-existing

      --reset-session

      --allow-sensitive-tools

  -h, --help
          Print help (see a summary with '-h')

Global Options:
      --profile <PROFILE>
          Use the named CLI profile from the local profile registry

      --expect-profile <EXPECT_PROFILE>
          Require the resolved active profile name to match this value

      --config <CONFIG_PATH>
          Use this palyra.toml config file

      --state-root <STATE_ROOT>
          Use this runtime state root directory

  -v, --verbose...
          Increase logging verbosity (-v => debug, -vv => trace)

      --log-level <LOG_LEVEL>
          Set diagnostic log verbosity

          [default: info]
          [possible values: error, warn, info, debug, trace]

      --output-format <OUTPUT_FORMAT>
          Select text, JSON, or NDJSON output for automation

          [default: text]
          [possible values: text, json, ndjson]

      --plain
          Disable styled terminal output

      --no-color
          Disable ANSI color output

      --allow-profile-mismatch
          Allow commands to continue when the selected profile differs from context

      --allow-strict-profile-actions
          Permit strict profile-scoped actions after explicit profile checks

Examples:
  palyra acp
  palyra acp --session-key ops:triage --session-label "Ops Triage"
  palyra acp --require-existing
  palyra acp shim --session-id 01ARZ3NDEKTSV4RRFFQ69G5FAW --prompt "hello"
  palyra acp shim --ndjson-stdin

Discoverability:
  `acp` is the preferred ACP bridge entry point. `palyra agent acp` and `palyra agent acp-shim` remain compatible.
  CLI defaults for `session_key`, `session_label`, `require_existing`, and `reset_session` seed bridge behavior; `_meta` prompt overrides still win when present.
