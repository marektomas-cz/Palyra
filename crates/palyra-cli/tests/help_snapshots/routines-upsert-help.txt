Usage: palyra routines upsert [OPTIONS] --name <NAME> --prompt <PROMPT> --trigger-kind <TRIGGER_KIND>

Options:
      --id <ULID>
          Canonical routine ULID. Omit to generate one; use --name for a human-readable label
      --name <NAME>

      --prompt <PROMPT>

      --trigger-kind <TRIGGER_KIND>
          [possible values: schedule, hook, webhook, system-event, manual]
      --owner <OWNER>

      --channel <CHANNEL>

      --session-key <SESSION_KEY>

      --session-label <SESSION_LABEL>

      --enabled <ENABLED>
          [possible values: true, false]
      --natural-language-schedule <NATURAL_LANGUAGE_SCHEDULE>

      --schedule-type <SCHEDULE_TYPE>
          [possible values: cron, every, at]
      --schedule <SCHEDULE>
          Schedule payload: cron expression for cron, RFC3339 timestamp for at, or milliseconds/duration such as 300000 or 5m for every
      --trigger-payload <TRIGGER_PAYLOAD>

      --trigger-payload-stdin

      --concurrency <CONCURRENCY>
          [default: forbid] [possible values: forbid, replace, queue-one]
      --retry-max-attempts <RETRY_MAX_ATTEMPTS>
          [default: 1]
      --retry-backoff-ms <RETRY_BACKOFF_MS>
          [default: 1000]
      --misfire <MISFIRE>
          [default: skip] [possible values: skip, catch-up]
      --jitter-ms <JITTER_MS>
          [default: 0]
      --delivery-mode <DELIVERY_MODE>
          [default: same-channel] [possible values: same-channel, specific-channel, local-only, logs-only]
      --delivery-channel <DELIVERY_CHANNEL>

      --delivery-failure-mode <DELIVERY_FAILURE_MODE>
          [possible values: same-channel, specific-channel, local-only, logs-only]
      --delivery-failure-channel <DELIVERY_FAILURE_CHANNEL>

      --silent-policy <SILENT_POLICY>
          [default: noisy] [possible values: noisy, failure-only, audit-only]
      --run-mode <RUN_MODE>
          [default: same-session] [possible values: same-session, fresh-session]
      --procedure-profile-id <PROCEDURE_PROFILE_ID>

      --skill-profile-id <SKILL_PROFILE_ID>

      --provider-profile-id <PROVIDER_PROFILE_ID>

      --execution-posture <EXECUTION_POSTURE>
          [default: standard] [possible values: standard, sensitive-tools]
      --quiet-hours-start <QUIET_HOURS_START>

      --quiet-hours-end <QUIET_HOURS_END>

      --quiet-hours-timezone <QUIET_HOURS_TIMEZONE>
          [possible values: local, utc]
      --cooldown-ms <COOLDOWN_MS>
          [default: 0]
      --approval-mode <APPROVAL_MODE>
          [default: none] [possible values: none, before-enable, before-first-run]
      --template-id <TEMPLATE_ID>

      --json

  -h, --help
          Print help

Global Options:
      --profile <PROFILE>
          Use the named CLI profile from the local profile registry
      --expect-profile <EXPECT_PROFILE>
          Require the resolved active profile name to match this value
      --config <CONFIG_PATH>
          Use this palyra.toml config file
      --state-root <STATE_ROOT>
          Use this runtime state root directory
  -v, --verbose...
          Increase logging verbosity (-v => debug, -vv => trace)
      --log-level <LOG_LEVEL>
          Set diagnostic log verbosity [default: info] [possible values: error, warn, info, debug, trace]
      --output-format <OUTPUT_FORMAT>
          Select text, JSON, or NDJSON output for automation [default: text] [possible values: text, json, ndjson]
      --plain
          Disable styled terminal output
      --no-color
          Disable ANSI color output
      --allow-profile-mismatch
          Allow commands to continue when the selected profile differs from context
      --allow-strict-profile-actions
          Permit strict profile-scoped actions after explicit profile checks
